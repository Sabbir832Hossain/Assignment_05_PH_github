**********\*\*\***********//**_Start_**//**********\*\*\*********** 1️⃣ What is
the difference between var, let, and const? //Ans to the question no 01 :-

Var let and const are used to declare variables in JavaScript. Var has function
scope and it is hoisted. Let and const have block scope. Lat can reassigned but
const can't be reassigned after declaration. Const is commonly used for values
that should not change.

2️⃣ What is the spread operator (...)? Ans to the question no 02 :-

The rest operator (...) collects multiple elements into a single array.

3️⃣ What is the difference between map(), filter(), and forEach()? Ans to the
question no 03 :-

Map() creates a new array by applying a function to each element of an array.
Filter creates a new array based on a condition For each is used to iterate
through an array. But it does not return a new array.

4️⃣ What is an arrow function? Ans to the question no 04 :-

Arrow function are a modern way to write function in Java Script. Provide
shorter syntax compared to regular functions. If the function has only on
expression, the return keyword can be omitted.

5️⃣ What are template literals? Ans to the question no 05 :-

Template literals used Backticks(``); They allow string interpolation and
multiline string.

**********\*\***********//**_END_**//**********\*\*\*\***********
